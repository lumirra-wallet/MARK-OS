# SmartAgent dependencies.
#
# Kept minimal for the initial scaffold — only what's needed to run the
# placeholder project and its test suite. Real dependencies (an LLM SDK,
# a memory/vector store client, speech libraries, a scheduler, etc.) will
# be added here as each feature is implemented.

pytest>=8.0
pytest

# Loads .env (GITHUB_TOKEN, OPENAI_API_KEY, ACTIVE_PROVIDER, etc.) into the
# real process environment at server startup — see smartagent/server/app.py.
python-dotenv>=1.0

# GitHub Models' API is OpenAI-compatible, so GitHubProvider (smartagent/llm/)
# uses this SDK under the hood; also used by the OpenAI TTS provider
# (smartagent/tts/openai_provider.py) and the existing /voice/speak-openai route.
openai>=1.0

# Browser automation for MARK's preview self-inspection (smartagent/preview/).
# Uses the system-installed Chromium (see browser_agent._find_chromium_executable),
# not Playwright's own bundled-browser download.
playwright>=1.40

# Default TTS provider (smartagent/tts/kokoro_provider.py) — CPU/ONNX runtime,
# matches this project's local-first design. Model weights (~115MB, the
# quantized int8 variant) are downloaded separately into .mark_tts_models/
# (gitignored) from https://github.com/thewh1teagle/kokoro-onnx/releases —
# not bundled in this package.
kokoro-onnx>=0.4
