"""
Vision subpackage.

Handles visual perception so SmartAgent can eventually understand images
and video (e.g. describing a photo, reading a screenshot). Kept separate
from `brain` so vision can remain fully optional — text-only usage should
always work without this subpackage doing anything.
"""
