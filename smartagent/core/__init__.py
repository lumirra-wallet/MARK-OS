"""
Core subpackage.

Contains the central "brain" of SmartAgent: the orchestration logic that
receives a user request, consults memory, decides whether to invoke tools,
and produces a response. Everything else (voice, automation, config) feeds
into or is driven by this layer.
"""
