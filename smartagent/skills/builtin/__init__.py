"""
Built-in skills — Milestone 3, Part 4.

Every module in this package is auto-discovered by
`smartagent.skills.skill_loader.discover_skill_classes()` (Part 7) — this
`__init__.py` deliberately does not import any of them, so adding a new
skill file here is enough to register it; nothing needs to be edited in
this file or in `skill_engine.py`.

Each skill below works only with existing project modules (memory,
planning, research) and uses no AI/model calls and no internet access,
per Milestone 3's rules.
"""
